pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.{js,ts,jsx,tsx}"
      run: pnpm lint --max-warnings=0 -- {staged_files}

    - name: typecheck
      run: pnpm tsc -b

    - name: format
      glob: "*.{js,ts,jsx,tsx,json}"
      run: pnpm fmt:check -- {staged_files}
